# Agent Office - Social Network Information Spread Simulation
# Core system uses only Python standard library

# Optional: GUI visualization (tkinter)
# On most systems, tkinter comes with Python. Install if needed:
# Ubuntu/Debian: sudo apt-get install python3-tk
# Fedora: sudo dnf install python3-tkinter
# macOS: Usually included with Python from python.org
# Windows: Usually included with Python installer

# Future dependencies for enhanced ML pipeline:
# numpy>=1.20.0
# pandas>=1.3.0
# matplotlib>=3.4.0
# networkx>=2.6.0
# scikit-learn>=0.24.0
